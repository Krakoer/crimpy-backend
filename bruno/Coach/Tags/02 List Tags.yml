info:
  name: List Tags
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/coach/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
