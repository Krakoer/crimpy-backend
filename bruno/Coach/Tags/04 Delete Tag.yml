info:
  name: Delete Tag
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/coach/tags/{{tagId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
