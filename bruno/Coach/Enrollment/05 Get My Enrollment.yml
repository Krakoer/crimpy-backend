info:
  name: Get My Enrollment
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/user/enrollment"
  auth:
    type: bearer
    token: "{{authTokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
