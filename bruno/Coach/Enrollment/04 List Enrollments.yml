info:
  name: List Enrollments
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/coach/enrollments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
