info:
  name: Accept Enrollment
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/enrollment/{{enrollmentToken}}/accept"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
