info:
  name: Unenroll User
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/coach/enrollments/{{clientUserId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
