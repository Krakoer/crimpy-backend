info:
  name: Get Token Info
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/enrollment/{{enrollmentToken}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenUser}}"
