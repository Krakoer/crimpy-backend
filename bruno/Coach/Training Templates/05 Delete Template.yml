info:
  name: Delete Training Template
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/coach/trainings/{{coachTrainingId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
