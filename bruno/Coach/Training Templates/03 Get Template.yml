info:
  name: Get Training Template
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/coach/trainings/{{coachTrainingId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
