info:
  name: List Training Templates
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/coach/trainings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
