info:
  name: List Favorite Exercises
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/coach/exercises/favorites"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
