info:
  name: Get Exercise
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/coach/exercises/{{exerciseId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
