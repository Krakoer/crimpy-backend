info:
  name: Get Exercise
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/coach/exercises/{{exerciseId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
