info:
  name: Unassign Tag
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/coach/exercises/{{exerciseId}}/tags/{{tagId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
