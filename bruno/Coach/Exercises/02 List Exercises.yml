info:
  name: List Exercises
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/coach/exercises"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
