info:
  name: List Client Sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
