info:
  name: List Client Assessments
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/assessments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
