info:
  name: List Weeks
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/programs/{{programId}}/weeks"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenCoach}}"
