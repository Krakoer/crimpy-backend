info:
  name: Upsert Week
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/programs/{{programId}}/weeks/{{weekNumber}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "notes": "First heavy week",
        "sessions": [
          {
            "training_id": "{{coachTrainingId}}",
            "day_of_week": 0,
            "notes": "Focus on max load",
            "overrides": [
              {
                "item_id": "{{coachTrainingItemId}}",
                "overrides": {
                  "loads": [{"value": 35, "unit": "kg"}],
                  "reps": 6
                }
              }
            ]
          },
          {
            "training_id": "{{coachTrainingId}}",
            "times_per_week": 2,
            "overrides": []
          }
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 200) {
          bru.setEnvVar("weekId", res.body.id);
          bru.setEnvVar("weekSessionId", res.body.sessions[0].id);
          bru.setEnvVar("weekSessionId2", res.body.sessions[1].id);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
