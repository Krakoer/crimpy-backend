info:
  name: List Programs
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/programs"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenCoach}}"
