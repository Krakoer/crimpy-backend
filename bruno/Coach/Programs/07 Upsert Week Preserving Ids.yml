info:
  name: Upsert Week Preserving Ids
  type: http
  seq: 7

http:
  method: PUT
  url: "{{baseUrl}}/api/coach/clients/{{clientUserId}}/programs/{{programId}}/weeks/{{weekNumber}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "notes": "First heavy week, notes fixed",
        "sessions": [
          {
            "id": "{{weekSessionId}}",
            "training_id": "{{coachTrainingId}}",
            "day_of_week": 0,
            "notes": "Focus on max load",
            "overrides": [
              {
                "item_id": "{{coachTrainingItemId}}",
                "overrides": {
                  "loads": [{"value": 35, "unit": "kg"}],
                  "reps": 6
                }
              }
            ]
          },
          {
            "id": "{{weekSessionId2}}",
            "training_id": "{{coachTrainingId}}",
            "times_per_week": 2,
            "overrides": []
          }
        ]
      }
  auth:
    type: bearer
    token: "{{authTokenCoach}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 200) {
          const kept = res.body.sessions[0].id === bru.getEnvVar("weekSessionId");
          if (!kept) {
            throw new Error("session id was not preserved across the save");
          }
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
