info:
  name: Get My Program
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/user/programs/{{programId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenUser}}"
