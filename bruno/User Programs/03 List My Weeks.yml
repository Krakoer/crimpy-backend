info:
  name: List My Weeks
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/user/programs/{{programId}}/weeks"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenUser}}"
