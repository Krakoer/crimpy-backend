info:
  name: Get Current User
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/user"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
