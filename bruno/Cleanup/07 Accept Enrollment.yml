info:
  name: Accept Enrollment
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/enrollment/{{enrollmentToken}}/accept"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authTokenUser}}"
