info:
  name: Pull
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/sync/pull"
  params:
    - name: since_version
      value: "0"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
