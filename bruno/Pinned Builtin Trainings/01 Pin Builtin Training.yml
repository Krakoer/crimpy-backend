info:
  name: Pin Builtin Training
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/pinned-builtin-trainings"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "builtin_training_id": "{{builtinTrainingId}}"
      }
