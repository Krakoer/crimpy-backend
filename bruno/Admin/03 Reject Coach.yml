info:
  name: Reject Coach
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/admin/coaches/{{userId}}/reject"
  body:
    type: none
  auth:
    type: bearer
    token: "{{authToken}}"
